<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>e-portfolio</title>

    <style>
      
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Poppins", sans-serif;
            transition: background 0.4s ease, color 0.4s ease;
        }

        /* --- THEMES --- */

        /* Thème clair */
        body.light {
            background: #f8f3f6;
            color: #4a3b3b;
        }

        /* Thème sombre */
        body.dark {
            background: #1a1416;
            color: #f2e6ea;
        }

        /* HEADER */
        .header {
            width: 100%;
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #ffffffcc;
            backdrop-filter: blur(6px);
            border-bottom: 1px solid #e8d8df;
        }

        body.dark .header {
            background: #2a1f22cc;
            border-bottom: 1px solid #5a3f46;
        }

        .logo {
            font-size: 1.7rem;
            font-weight: 600;
            color: #c06c84;
            letter-spacing: 1px;
        }

        .nav a {
            margin-left: 25px;
            text-decoration: none;
            color: inherit;
            font-weight: 500;
            transition: 0.3s;
        }

        .nav a:hover {
            color: #c06c84;
        }

        /* Bouton thème */
        .theme-btn {
            padding: 8px 18px;
            border-radius: 25px;
            border: 1px solid currentColor;
            background: transparent;
            cursor: pointer;
            font-weight: 500;
            transition: 0.3s;
        }

        .theme-btn:hover {
            opacity: 0.7;
        }

        /* HERO SECTION */
        .hero {
            height: 90vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #fce4ec, #f8bbd0, #f48fb1);
            padding: 20px;
            text-align: center;
        }

        body.dark .hero {
            background: linear-gradient(135deg, #3a2a2f, #5a3f46, #7a5560);
        }

        .hero-content {
            max-width: 700px;
        }

        .hero-title {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 15px;
        }

        .hero-text {
            font-size: 1.2rem;
            line-height: 1.6;
            margin-bottom: 30px;
        }

        /* BUTTON */
        .btn {
            display: inline-block;
            padding: 14px 32px;
            background: #c06c84;
            color: white;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            transition: 0.3s;
        }

        .btn:hover {
            background: #a85a72;
        }
        
footer {
    padding: 30px 10%;
    text-align: center;
    font-size: 0.9rem;
    border-top: 1px solid #e0c3cc;
}

body.dark footer {
    border-top: 1px solid #5a3f46;
}
    </style>
</head>

<body class="light">

    <header class="header">
        <h1 class="logo">e-portfolio</h1>

        
      

        <button class="theme-btn" onclick="toggleTheme()">Changer le thème</button>
    </header>

    <section class="hero">
        <div class="hero-content">
            <h2 class="hero-title">FATOU BINETOU DIALLO  GROUP A2</h2>
            <p class="hero-text">
                Welcome to my website. Here you find a Bachelor of Technology (BUT) student in Networks and Telecommunications, combining technical rigor with a strong understanding of modern communication infrastructures and network systems.
            </p>
             <a href="{{ path('pourensavoirplus') }}" class="btn primary">
    Discover more

</a>
        </div>
    </section>

    <script>
        function toggleTheme() {
            const body = document.body;
            body.classList.toggle("dark");
            body.classList.toggle("light");
        }
    </script>
    <!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
   

    <style>
        body {
            font-family: "Poppins", sans-serif;
            transition: background 0.4s ease, color 0.4s ease;
        }

        /* Thème clair */
        body.light {
            background: #f8f3f6;
            color: #4a3b3b;
        }

        /* Thème sombre */
        body.dark {
            background: #1a1416;
            color: #f2e6ea;
        }

        .theme-btn {
            padding: 8px 18px;
            border-radius: 25px;
            border: 1px solid currentColor;
            background: transparent;
            cursor: pointer;
            font-weight: 500;
            transition: 0.3s;
        }
    </style>
</head>

<body>

   

  

    <script>
        // Charger le thème enregistré
        const savedTheme = localStorage.getItem("theme");

        if (savedTheme) {
            document.body.classList.add(savedTheme);
        } else {
            document.body.classList.add("light"); // thème par défaut
        }

        // Fonction pour changer le thème
        function toggleTheme() {
            const body = document.body;

            if (body.classList.contains("light")) {
                body.classList.replace("light", "dark");
                localStorage.setItem("theme", "dark");
            } else {
                body.classList.replace("dark", "light");
                localStorage.setItem("theme", "light");
            }
        }
    </script>

</body>
</html>

<footer>
    © 2026 – e-portfolio | Tous droits réservés
</footer>

</body>
</html>
