<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>about</title>

    <style>
       
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Poppins", sans-serif;
            transition: background 0.4s ease, color 0.4s ease;
        }

       
        body.light {
            background: #f8f3f6;
            color: #4a3b3b;
        }

        body.dark {
            background: #1a1416;
            color: #f2e6ea;
        }

        /* HEADER */
        .header {
            width: 100%;
            padding: 20px 50px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #ffffffcc;
            backdrop-filter: blur(6px);
            border-bottom: 1px solid #e8d8df;
        }

        body.dark .header {
            background: #2a1f22cc;
            border-bottom: 1px solid #5a3f46;
        }

        .logo {
            font-size: 1.7rem;
            font-weight: 600;
            color: #c06c84;
            letter-spacing: 1px;
        }

        .nav a {
            margin-left: 25px;
            text-decoration: none;
            color: inherit;
            font-weight: 500;
            transition: 0.3s;
        }

        .nav a:hover {
            color: #c06c84;
        }

        /* Bouton thème */
        .theme-btn {
            padding: 8px 18px;
            border-radius: 25px;
            border: 1px solid currentColor;
            background: transparent;
            cursor: pointer;
            font-weight: 500;
            transition: 0.3s;
        }

        .theme-btn:hover {
            opacity: 0.7;
        }

       
        .hero {
            height: 80vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #fce4ec, #f8bbd0, #f48fb1);
            padding: 20px;
            text-align: center;
        }

        body.dark .hero {
            background: linear-gradient(135deg, #3a2a2f, #5a3f46, #7a5560);
        }

        .hero-content {
            max-width: 700px;
        }

        .hero-title {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 15px;
        }

        .hero-text {
            font-size: 1.2rem;
            line-height: 1.6;
            margin-bottom: 30px;
        }

        .btn {
            display: inline-block;
            padding: 14px 32px;
            background: #c06c84;
            color: white;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            transition: 0.3s;
        }

        .btn:hover {
            background: #a85a72;
        }

        
        .about {
            padding: 80px 10%;
            display: flex;
            gap: 50px;
            align-items: center;
        }

        .about-text {
            flex: 1;
        }

        .about-text h2 {
            font-size: 2.2rem;
            margin-bottom: 20px;
            color: #c06c84;
        }

        .about-text p {
            font-size: 1.1rem;
            line-height: 1.7;
        }

        .about-photo {
            flex: 1;
            display: flex;
            justify-content: center;
        }

        .about-photo div {
            width: 280px;
            height: 280px;
            border-radius: 20px;
            background: #f8bbd0;
            background-size: cover;
            background-position: center;
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        }

        body.dark .about-photo div {
            background: #5a3f46;
        }

        /* RESPONSIVE */
        @media (max-width: 900px) {
            .about {
                flex-direction: column;
                text-align: center;
            }
        }
        
.analyse {
    padding: 80px 10%;
    background: rgba(192, 108, 132, 0.08);
}

body.dark .analyse {
    background: rgba(122, 85, 96, 0.25);
}

.analyse h2 {
    color: #c06c84;
    margin-bottom: 25px;
    text-align: center;
}

.analyse p {
    max-width: 900px;
    margin: 0 auto 40px;
    line-height: 1.8;
    font-size: 1.1rem;
    text-align: center;
}

.analyse-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 25px;
}

.analyse-card {
    border-radius: 18px;
    overflow: hidden;
    box-shadow: 0 10px 25px rgba(0,0,0,0.15);
    background: white;
}

body.dark .analyse-card {
    background: #2a1f22;
}

.analyse-card img {
    width: 100%;
    height: 200px;
    object-fit: cover;
}

.analyse-card span {
    display: block;
    padding: 15px;
    font-weight: 500;
    text-align: center;
}


footer {
    padding: 30px 10%;
    text-align: center;
    font-size: 0.9rem;
    border-top: 1px solid #e0c3cc;
}

body.dark footer {
    border-top: 1px solid #5a3f46;
}

    </style>
</head>

<body class="light">

    <header class="header">
        <h1 class="logo">portfolio</h1>

        <nav class="nav">
             <a href="{{ path('accueil') }}" class="btn primary">
    Accueil
</a>
           <a href="{{ path('cv') }}" class="btn primary">
    CV
</a>          
   <a href="{{ path('pourensavoirplus') }}" class="btn primary">
      passions
</a>         
  
            <a href="{{ path('contact') }}" class="btn primary">
      Contact</a>
        </nav>

        <button class="theme-btn" onclick="toggleTheme()">Changer le thème</button>
    </header>

    <section class="hero">
        <div class="hero-content">
            <h2 class="hero-title">CLIQUEZ SUR LE LIEN POUR ACCEDER DIRECTEMENT A MON PORTFOLIO</h2>
            <p class="hero-text">
                
            </p>
           
            <a href="https://docs.google.com/spreadsheets/d/114HDEnFWbTSS2z9pYiL6No5QFBft2uk3/edit?gid=295771220#gid=295771220" 
           target="_blank" 
           class="btn">
           Voir mon portfolio
        </a
        </div>
    </section>

  
            <section class="analyse">
    <h2>Analyse réflexive</h2>

    <p>
        Ce portfolio représente l’évolution de mes compétences tout au long de mon parcours.
        À travers ces projets, j’ai développé ma créativité, ma maîtrise des outils numériques
        ainsi que ma capacité à concevoir des solutions visuelles cohérentes et professionnelles.
        Les éléments présentés ci-dessous constituent des preuves concrètes de mon travail,
        de mon engagement et de ma progression.
        voila un lien zip conduisant à l'achitecture du nouveau reseau que j'ai du creer pour mener a bien ma SAE1.02
    </p>

   <a href="/SAE12 packet tracer.zip" download>Télécharger le dossier</a>



        
    </div>
</section>

        </div>
    </section>

    <script>
        function toggleTheme() {
            document.body.classList.toggle("dark");
            document.body.classList.toggle("light");
            
        }
    </script>
    <!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Page avec thème persistant</title>

    <style>
        body {
            font-family: "Poppins", sans-serif;
            transition: background 0.4s ease, color 0.4s ease;
        }

        /* Thème clair */
        body.light {
            background: #f8f3f6;
            color: #4a3b3b;
        }

        /* Thème sombre */
        body.dark {
            background: #1a1416;
            color: #f2e6ea;
        }

        .theme-btn {
            padding: 8px 18px;
            border-radius: 25px;
            border: 1px solid currentColor;
            background: transparent;
            cursor: pointer;
            font-weight: 500;
            transition: 0.3s;
        }
    </style>
</head>

<body>

    

    

    <script>
     
        const savedTheme = localStorage.getItem("theme");

        if (savedTheme) {
            document.body.classList.add(savedTheme);
        } else {
            document.body.classList.add("light"); // thème par défaut
        }

        
        function toggleTheme() {
            const body = document.body;

            if (body.classList.contains("light")) {
                body.classList.replace("light", "dark");
                localStorage.setItem("theme", "dark");
            } else {
                body.classList.replace("dark", "light");
                localStorage.setItem("theme", "light");
            }
        }
    </script>

</body>
</html>
<footer>
    © 2026 – e-portfolio | Tous droits réservés
</footer>




</body>
</html>
